# Liquibase Global Configuration
# This file disables pro features to avoid license issues during development

# Disable pro features that require licensing
liquibase.pro.flow.enabled=false
liquibase.pro.checks.enabled=false
liquibase.pro.monitoring.enabled=false

# Basic logging configuration
liquibase.logLevel=INFO
liquibase.shouldRun=true
